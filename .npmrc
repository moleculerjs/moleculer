# The repo itself satisfies moleculer-repl's "moleculer" peer dependency, but npm
# tries to install the published moleculer package instead, whose peer ranges may
# conflict with the devDependencies (e.g. @platformatic/kafka 2.x).
legacy-peer-deps=true
